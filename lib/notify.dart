import 'package:flutter/material.dart';

class Notificationon extends StatelessWidget {
  const Notificationon({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Notification Settings'),
      ),
    );
  }
}
