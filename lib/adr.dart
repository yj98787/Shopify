import 'package:flutter/material.dart';

class Adressin extends StatelessWidget {
  const Adressin({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Saved Addresses'),
      ),
    );
  }
}
