import 'package:flutter/material.dart';

class Cardin extends StatelessWidget {
  const Cardin({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Saved Cards and Wallet'),
      ),
    );
  }
}
